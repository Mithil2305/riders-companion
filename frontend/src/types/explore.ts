export interface SuggestedUser {
  id: string;
  name: string;
  avatar: string;
}

export interface SuggestedRoom {
  id: string;
  name: string;
  members: number;
}

export interface TrendingClip {
  id: string;
  title: string;
  thumbnail: string;
}
